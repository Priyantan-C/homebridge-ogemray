/** Platform name registered with Homebridge; must match config.json `platform`. */
export const PLATFORM_NAME = "Ogemray";

/** npm package name; must match the name in package.json. */
export const PLUGIN_NAME = "homebridge-ogemray";
