{
  "pluginAlias": "Ogemray",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Local HomeKit control for Ogemray / Tata Power EZ Home switches. Devices on your LAN are discovered automatically — no account, no credentials, no cloud.",
  "footerDisplay": "Devices are found via a UDP broadcast, so Homebridge must be on the same network segment as the switches.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Ogemray",
        "required": true,
        "description": "Platform name shown in the Homebridge log."
      }
    }
  }
}
